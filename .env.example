# Summarizer app: use model from Azure Blob Storage (production).
# Copy to .env and set your blob URL (with SAS token if container is private).
# Get the URL after uploading model.zip via the notebook "Upload model to Azure Blob Storage" cell.

AZURE_MODEL_BLOB_URL=''
AZURE_STORAGE_CONNECTION_STRING=''
# Optional: override only if not using Azure
# MLFLOW_MODEL_URI=models:/Flan-T5-Summarization@champion
# MAX_NEW_TOKENS=80
# FLASK_DEBUG=1   # set for local dev (enables debug mode and detailed error messages)
